@page
@model LogoutModel
@{
    ViewData["Title"] = T["Log out"];
}

<header>
    <h1>@ViewData["Title"]</h1>
    @{
        if (User.Identity.IsAuthenticated)
        {
            <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Page("/", new { area = "" })" method="post">
                <button type="submit" class="nav-link btn btn-link text-dark">@T["Click here to Logout"]</button>
            </form>
        }
        else
        {
            <p>@T["You have successfully logged out of the application."]</p>
        }
    }
</header>