@model DuoPoll.Dal.Entities.Poll
@{
    ViewData["Title"] = "Polls";
    ViewData["Current"] = "polls.show";
}

<div class="grid grid-cols-12 text-center align-baseline gap-5 md:gap-10 max-w-sm md:max-w-4xl mx-auto">
    <div class="@Html.DisplayName((bool) ViewData["Owner"] ? "col-span-10 text-right" : "col-span-12") font-body text-3xl md:text-5xl">
        Some interesting question?
    </div>
    @if ((bool) ViewData["Owner"])
    {
        <div class="col-span-2 font-body text-lg md:text-xl flex justify-end self-end">
            <a asp-controller="Poll" asp-action="Edit" asp-route-url="@Model.Url">
                <t-button variant="dark">
                    Edit
                </t-button>
            </a>
        </div>
    }
    <div class="col-span-12">
        <vote url="/api/Answer/@Model.Url"></vote>
    </div>
</div>